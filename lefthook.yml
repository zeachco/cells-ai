pre-commit:
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --all --check
    clippy:
      glob: "*.rs"
      run: cargo clippy --target wasm32-unknown-unknown -- -D warnings
    check:
      glob: "*.rs"
      run: cargo check
